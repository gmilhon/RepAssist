# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Local data (regenerated on run) — path is cwd-relative, so ignore at any depth
*.sqlite
*.sqlite-shm
*.sqlite-wal
repassist.db
backend/.env
system_health.json

# Node / Vite
frontend/node_modules/
frontend/dist/

# Built frontend bundle (regenerated into the image by deploy.sh)
backend/static/

# OS
.DS_Store

# Claude Code (local only — launch.json is tracked)
.claude/settings.local.json
